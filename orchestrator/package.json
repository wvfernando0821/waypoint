{
  "name": "waypoint-orchestrator",
  "version": "0.1.0",
  "description": "M3 proof-of-concept: Node API + Postgres + BullMQ/Redis wrapping the Waypoint analysis engine as an async job",
  "type": "module",
  "private": true,
  "scripts": {
    "migrate": "node --env-file=.env db/migrate.js",
    "start": "node --env-file=.env src/server.js"
  },
  "dependencies": {
    "bullmq": "^5.34.0",
    "express": "^4.21.0",
    "ioredis": "^5.4.0",
    "pg": "^8.13.0",
    "waypoint-engine": "*"
  }
}
